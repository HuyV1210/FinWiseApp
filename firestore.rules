rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary: Allow authenticated users to read/write all documents
    // TODO: Restrict to user-specific data later
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
